# =============================================================================
# Server-side secrets (Cloudflare Pages Secrets — NEVER prefixed with VITE_)
# Set these via: wrangler pages secret put SECRET_NAME
# =============================================================================

# Airtable
AIRTABLE_API_KEY=pat...
AIRTABLE_BASE_ID=app...

# RedTrack
REDTRACK_API_KEY=your_redtrack_api_key
REDTRACK_BASE_URL=https://api.redtrack.io

# Facebook Graph API (App Secret is the critical secret here)
FB_APP_ID=your_facebook_app_id
FB_APP_SECRET=your_facebook_app_secret

# Authentication
JWT_SECRET=generate_a_random_64_char_string

# =============================================================================
# Client-side config (VITE_ prefix — embedded in browser bundle, NOT secret)
# =============================================================================

# Cloudflare R2 storage
VITE_CF_STORAGE_WORKER_URL=https://your-worker.workers.dev
VITE_CF_R2_PUBLIC_URL=https://pub-xxx.r2.dev
VITE_CF_R2_DOMAIN=yourdomain.com

# Facebook App ID (public identifier — safe for client-side)
VITE_FB_APP_ID=your_facebook_app_id

# Feature flags
VITE_ENABLE_REAL_CAMPAIGN_LAUNCH=false

# Legacy (Google Drive — being migrated away)
VITE_GOOGLE_DRIVE_PRODUCTS_ROOT_ID=
